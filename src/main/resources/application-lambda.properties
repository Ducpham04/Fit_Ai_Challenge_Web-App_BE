spring.application.name=FIT_Challenge

# Database Configuration - Use Environment Variables
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://fitaichallengedb.chy8a8q2a5no.ap-southeast-1.rds.amazonaws.com:3306/fit_challenge?useSSL=true&requireSSL=false&serverTimezone=UTC}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:admin}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:6R2KEAXHtuiRM6kHcCaF}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection Pool Settings for Lambda
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA Configuration
spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT Configuration - Use Environment Variables
jwt.secret=${JWT_SECRET:du-an-safjhdskfjskjliuuasdhaos-2321}
jwt.expiration=${JWT_EXPIRATION:8640000}

# File Upload Configuration
upload.path=${UPLOAD_PATH:uploads/}
spring.servlet.multipart.max-file-size=3000MB
spring.servlet.multipart.max-request-size=3000MB

# Logging
logging.level.root=INFO
logging.level.com.example.fitchallenge=DEBUG
logging.level.org.springframework.security=INFO